<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bio.mapper.UserMapper">
    <select id="getOneByUsername" resultType="com.example.bio.model.User">
        select id,
               username,
               password,
               email,
               invitation_code,
               points,
               is_locked,
               gmt_create,
               gmt_modified
        from biography.user
        where username = #{username}
    </select>

    <select id="getOneByEmail" resultType="com.example.bio.model.User">
        select id,
               username,
               password,
               email,
               invitation_code,
               points,
               is_locked,
               gmt_create,
               gmt_modified
        from biography.user
        where email = #{email}
    </select>

    <update id="unlockUser" parameterType="com.example.bio.model.User">
        update biography.user
        set is_locked=0
        where id = #{id}
    </update>
</mapper>
