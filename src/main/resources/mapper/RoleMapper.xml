<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bio.mapper.RoleMapper">

    <select id="getByRoleName" resultType="com.example.bio.model.Role">
        SELECT id, role_name, is_deleted, gmt_create, gmt_modified
        FROM biography.role
        WHERE role_name = #{eRole}
    </select>

    <insert id="addRole">
        <foreach collection="roles" item="item" separator=";">
            INSERT INTO biography.user_role(user_id, role_id)
            VALUES (#{userId}, #{item.id})
        </foreach>
    </insert>

    <select id="getRoleByUserId" resultType="com.example.bio.model.Role">
        SELECT r.id,
               r.role_name,
               r.is_deleted,
               r.gmt_create,
               r.gmt_modified
        FROM biography.role r
                 INNER JOIN biography.user_role ur ON r.id = ur.role_id
        WHERE r.is_deleted = 0
          AND ur.is_deleted = 0
          AND ur.user_id = #{userId}
    </select>

    <!-- 删除用户指定角色（软删除 user_role 关联记录） -->
    <update id="deleteRole">
        UPDATE biography.user_role
        SET is_deleted = 1
        WHERE user_id = #{userId}
          AND role_id IN
              <foreach collection="roles" item="item" open="(" separator="," close=")">
                  #{item.id}
              </foreach>
    </update>

</mapper>
