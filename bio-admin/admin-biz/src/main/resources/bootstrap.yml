server:
  port: 8080
spring:
  application:
    name: bio-admin
  cloud:
    nacos:
      discovery:
        server-addr: http://192.168.123.47:8848
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yaml